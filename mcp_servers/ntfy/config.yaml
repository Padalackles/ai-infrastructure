# ──────────────────────────────────────────
# ntfy MCP — External Service Configuration
# ──────────────────────────────────────────

server:
  url: "${NTFY_BASE_URL:-https://ntfy.sh}"

notification:
  default_topic: "${NTFY_TOPIC:-ai-infrastructure}"
