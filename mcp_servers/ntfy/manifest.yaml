# ntfy MCP — External push notification service
#
# ntfy is an external service. The Hub connects via HTTP adapter.
# Default: https://ntfy.sh, topic: raven-victor-c503460d

name: ntfy
version: "0.2.0"
class: NtfyServer
description: Push notification service — ntfy.sh via curl on VPS host
type: external_service
health_endpoint: /health
tools:
  - notify_send
  - ntfy_health
  - ntfy_info
