# ntfy MCP — External push notification service
#
# ntfy is an external service. The Hub connects via HTTP adapter.
# Default: https://ntfy.sh, topic: ai-infrastructure

name: ntfy
version: "0.1.0"
class: NtfyServer
description: External push notification service — ntfy.sh API integration
type: external_service
health_endpoint: /health
tools:
  - ntfy_health
  - ntfy_info
  - ntfy_send
