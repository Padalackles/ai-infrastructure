# ──────────────────────────────────────────
# MCP Hub — Configuration
# ──────────────────────────────────────────

server:
  host: "0.0.0.0"
  port: 8080

logging:
  level: "INFO"

# MCP servers are auto-discovered from mcp_servers/ directory.
# Manual per-server overrides can be added here when needed:
#
# mcp_servers:
#   ombre:
#     enabled: true
#   ntfy:
#     enabled: true
