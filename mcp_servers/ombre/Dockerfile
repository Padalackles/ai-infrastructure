# ── Ombre MCP Server — Environment ─────────
FROM node:20-alpine

WORKDIR /app
# TODO: add package.json and source code
# COPY package.json package-lock.json ./
# RUN npm ci
# COPY . .

EXPOSE 3000
CMD ["echo", "Ombre MCP — not yet implemented"]
