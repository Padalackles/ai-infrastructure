# ── MCP Hub — Environment ──────────────────
FROM python:3.12-slim

WORKDIR /app
# TODO: add requirements.txt and source code
# COPY requirements.txt .
# RUN pip install -r requirements.txt
# COPY . .

EXPOSE 8080
CMD ["echo", "MCP Hub — not yet implemented"]
